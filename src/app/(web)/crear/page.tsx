import { PlantillaView } from "@/smiles/plantilla";

export default function CrearPage() {
  return (
    <PlantillaView
      etiqueta="Crear"
      titulo="Creador de mensajes"
      descripcion="Aqui ira tu formulario principal para guardar mensajes en Supabase."
    />
  );
}
