import { PlantillaView } from "@/smiles/plantilla";

export default function GestorPage() {
  return (
    <PlantillaView
      etiqueta="Gestor"
      titulo="Panel Gestor"
      descripcion="Vista de gestion rapida para mensajes, revision y control operativo."
    />
  );
}
